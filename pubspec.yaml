name: brick_issue_309
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: 3.3.8

dependencies:
  bloc: ^8.1.0
  brick_offline_first_with_rest: ^1.1.0
  brick_sqlite: ^2.1.0
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0
  sqflite: ^2.2.2

dev_dependencies:
  brick_offline_first_with_rest_build: ^2.1.0
  build_runner: ^2.3.3
  flutter_gen_runner: ^5.1.0+1
  very_good_analysis: ^3.1.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/images/
